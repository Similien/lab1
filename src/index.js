
//eslint-disable-next-line
import React from 'react';
import ReactDOM from 'react-dom';
import  App from "./components/app"



ReactDOM.render(
    App(), 
    document.getElementById('root')
);



/* function dateOp(){
  let options = {
    weekday:'long',
    year:'numeric', 
    month:'long',
    day:'numeric'
  }
  let d = new Date();
  let daDay = d.toLocaleDateString("en-US");
  test.innerHTML = "&copy " + daDay;
}


 */











/* function create(el, txtNd){
  el = document.createElement(el)
  txtNd = document.createTextNode(txtNd);
  el.appendChild(txtNd);
  div.appendChild(el)
}


create("h1", "msg");
 */
